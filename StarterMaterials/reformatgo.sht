#!/bin/sh

# Purpose:
# Reformat file system object:
# {-x=xml file, -r=Rick file, -d=idb}.
#
# > reformat path <-x,-r,-d> -p=_Nex.mrc -zmin=i -zmax=j
#
# Required -p= pattern describes the context around
# the tile ID 'N' part of image names.


reformat rick0_v2.txt -r -p=_N_ -zmin=0 -zmax=2

